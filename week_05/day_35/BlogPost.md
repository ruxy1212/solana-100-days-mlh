This week I created a custom SPL token on Solana with metadata, transfer fees, and non-transferable extensions!

I didn't expect the Token Extensions Program to let you enforce economic rules directly at the protocol level, no off-chain logic needed.

#100DaysOfSolana @solana_devs