# 100 Days of Solana

100 days of Solana challenge on MLH.

## Run

1. Install dependencies:
   - `pnpm install`
2. Add `.env`:
   - `KEY=your_key_here`
3. Start:
   - `node path-to-file.ext`
